{
  "name": "@sol-a-sol/contracts",
  "version": "0.2.0",
  "private": true,
  "description": "Esquemas Zod compartidos entre la API y la web: la forma de lo que viaja por HTTP.",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@sol-a-sol/config": "workspace:*",
    "@vitest/coverage-v8": "5.0.0",
    "eslint": "10.10.0",
    "typescript": "6.0.3",
    "vitest": "5.0.0"
  },
  "dependencies": {
    "zod": "4.6.5"
  }
}
