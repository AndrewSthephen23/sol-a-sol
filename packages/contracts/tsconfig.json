{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@sol-a-sol/config/tsconfig/library.json",
  "compilerOptions": {
    // Los contratos los comparten la API (Node) y la web (navegador): sin tipos de ninguno
    // de los dos, para que no se cuele nada propio de un entorno.
    "types": [],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
