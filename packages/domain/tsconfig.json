{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@sol-a-sol/config/tsconfig/library.json",
  "compilerOptions": {
    // Dominio puro: sin tipos de Node ni del navegador. Usar `process`, `URL`, `fetch`, etc.
    // no compila, así que la regla se verifica automáticamente.
    "types": [],
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src"]
}
